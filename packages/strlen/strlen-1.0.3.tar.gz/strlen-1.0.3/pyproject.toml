[tool.poetry]
name = "strlen"
version = "1.0.3"
description = "Simple class that allows you to sort and compare strings by their length"
authors = ["Romooper <romanshupik32@gmail.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.6"
numpy = "^1.26.4"

[tool.poetry.group.dev.dependencies]
numpy = "^1.26.4"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
