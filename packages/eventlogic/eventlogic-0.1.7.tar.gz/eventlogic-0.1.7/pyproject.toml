[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "eventlogic"
version = "0.1.7"
dependencies = [
    "numpy",
    'importlib-metadata; python_version>="3.6"',
]
