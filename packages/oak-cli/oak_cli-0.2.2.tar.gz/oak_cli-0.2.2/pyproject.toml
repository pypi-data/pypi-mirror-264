[tool.poetry]
name = "oak_cli"
version = "0.2.2"
description = ""
authors = ["Alexander Malyuk <malyuk.alexander1999@gmail.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.10"
argcomplete = "^3.2.3"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"

[tool.poetry.scripts]
oak = "oak_cli.main:main"

[tool.poetry.plugins."poetry.plugin.activate"]
activate = "oak_cli.postinstall:main"
