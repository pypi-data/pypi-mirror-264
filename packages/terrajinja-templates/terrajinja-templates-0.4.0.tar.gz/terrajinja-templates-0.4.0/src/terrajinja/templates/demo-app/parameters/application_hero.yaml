# applications contains all applications we can deploy
applications:
  # This is the application called Evil
  hero:
    tla: her
    environments:

      test:
        # cloud provider this app till be deployed in for this environment
        cloud_provider: sbp.cloud
        # network details
        network:
          name: dmz
          ips: [10.0.0.6, 10.0.0.7]
        # not used in the demo, but for inspiration
        chef_run_list:
          - hero_app