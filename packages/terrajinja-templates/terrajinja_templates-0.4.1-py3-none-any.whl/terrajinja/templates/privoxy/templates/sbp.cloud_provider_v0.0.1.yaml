terraform:
  providers:
    - task: vcd-provider
      module: vcd.provider
      parameters:
        auth_type: api_token
        url: https://portal.sbp.cloud/api
        org: {{ team.tla }}
        api_token: "{{ env['SBP_CLOUD_API_TOKEN'] }}"
        max_retry_timeout: 30