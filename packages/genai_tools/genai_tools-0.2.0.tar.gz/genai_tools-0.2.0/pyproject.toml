[project]
name = "genai_tools"
version = "0.2.0"
description = "Default template for PDM package"
authors = [
    { name = "mavilam", email = "evobanco@evobanco.com" },
]
dependencies = []
requires-python = ">=3.10"
readme = "README.md"

[project.license]
text = "MIT"

[build-system]
requires = [
    "pdm-backend",
]
build-backend = "pdm.backend"

[tool.pdm]
distribution = true
