from setuptools import setup, find_packages

setup(
    name="timos_tools",
    version="0.0.2",
    packages=find_packages(include=["timos_tools", "timos_tools.json"]),
    author="timos",
    description="timos_tools",
    install_requires=[],
)
