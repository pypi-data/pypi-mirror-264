# storage 服务监听的地址和端口
addr: '127.0.0.1:9001'

compiler:
  cmds: ["./hbc", "@", "-o", "@"]

# 私有数据库相关配置, 目前只支持mysql
db:
  addr: '127.0.0.1'
  port: 3306
  database: storage1
  user: root
  password: '123456'

# 区块链配置, 目前支持长安链
chain:
  chainmaker:
    # 长安链的sdk配置文件
    sdk_config: ./testdata/sdk_config1.yaml
    # 长安链链上合约的名称
    contract_name: example
