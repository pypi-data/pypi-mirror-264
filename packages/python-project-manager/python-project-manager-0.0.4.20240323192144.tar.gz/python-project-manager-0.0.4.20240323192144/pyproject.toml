[build-system]
requires = [ "setuptools>=61.0",]
build-backend = "setuptools.build_meta"

[project]
name = "python-project-manager"
version = "0.0.4.20240323192144"

[project.scripts]
ppm = "python_project_manager.app:cli"
