name: pyQParamWidget

# conda env create --force -n pyQParamWidget -f ./env.yml --solver=libmamba

channels:
  - conda-forge
  - defaults

dependencies:
  - python >=3.10
  - pip
  - pyqt =5
  - qt =5
