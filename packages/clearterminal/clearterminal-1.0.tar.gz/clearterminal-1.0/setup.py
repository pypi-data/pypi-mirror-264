from setuptools import setup

setup(name='clearterminal',
      version='1.0',
      description='Clear your terminal with ONE COMMAND',
      packages=['clearterminal'],
      author_email='holinim@duck.com',
      zip_safe=False)