[tool.poetry]
name = "lazy-redis-log"
version = "0.1.3"
description = "Like print, but also writes to Redis Streams!"
authors = ["MisterNyan <michael.edmund.wong@gmail.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.8"
redis = "^5.0.2"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
